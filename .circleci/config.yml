version: 2.1

jobs:
  test:
    macos:
      xcode: 13.1.0 # Xcode 13 or higher required to compile new Swift concurrency
    steps:
      - checkout
      - run:
          # Build and Test the Typesense Package
          name: Run Tests
          command: swift test

workflows:
  version: 2
  test_build:
    jobs:
      - test
